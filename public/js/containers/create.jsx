import React from 'react';
const { Component } = React;

export default class Create extends Component {
    render() {
        return (
            <div className="page">
                Create new
            </div>
        );
    }
}
